import { createTheme } from "@mui/material/styles";

export const casinoTheme = createTheme({
  palette: {
    mode: 'dark',
    primary: {
      main: '#670090',
    },
    secondary: {
      main: '#C9AD5E',
    },
  },
});